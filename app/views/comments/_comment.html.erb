<div class="media">
  <div class="media-left">
    <%= image_tag(comment.user.avatar.small.url, width: 30, height: 30) %>
  </div>
  <div class="media-body">
    <h4 class="media-heading"><%= comment.user.name %></h4>
    <small>
      posted <%= time_ago_in_words(comment.created_at) %> ago
    </small>
    <p><%= comment.body %></p>
  </div>
</div>